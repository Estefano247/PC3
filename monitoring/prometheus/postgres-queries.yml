pg_stat_activity:
  query: "SELECT application_name, state, count(*) AS connections FROM pg_stat_activity WHERE datname IS NOT NULL AND state IS NOT NULL GROUP BY application_name, state ORDER BY application_name"
  metrics:
    - connections:
        usage: "GAUGE"
        description: "Number of connections by application name and state"
